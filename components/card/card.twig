{% set classes = [
    'card',
    'is-' ~ orientation|clean_class,
  ]
%}

<div {{ classes|join(' ') }}>
  <img src="{{ '/' ~ componentMetadata.path ~ '/img/' ~ mediaImage }}" alt="Thumbnail image" />
  <h2 class="card__heading">{{ heading }}</h2>
  <div class="card__text">{{ text }}</div>
</div>
