<div class="logo-tile {{ background|default('dark') == 'dark' ? 'bg-brand-dark' : 'bg-brand-light'  }} aspect-square">
  <div class="p-[10%] h-[100%] flex justify-center items-center">
    <img src="{{ '/' ~ componentMetadata.path ~ '/' ~ image.src }}" alt="{{ image.alt }}" />
  </div>
</div>
