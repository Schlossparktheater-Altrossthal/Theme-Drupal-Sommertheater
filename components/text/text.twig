{% set classes = [
    'rich-text',
  ]
%}

<div class="hg:text-base hg:lg:text-lg hg:2xl:text-xl hg:text-inherit {{ classes|join(' ') }}{{ font_color ? ' ' ~ font_color }}{{ font_size_multiplier == 'default' ? '' : ' ' ~ font_size_multiplier }}">
  {{ text|raw }}
</div>
