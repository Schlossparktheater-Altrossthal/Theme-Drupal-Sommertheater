{# <img src="{{ '/static/images/' ~ image.src }}" alt="{{ image.alt }}" width="{{ image.width }}" height="{{ image.height }}"></img> #}

<img src="{{ '/' ~ componentMetadata.path ~ '/' ~ image.src }}" alt="{{ image.alt }}" width="{{ image.width }}" height="{{ image.height }}"></img>
