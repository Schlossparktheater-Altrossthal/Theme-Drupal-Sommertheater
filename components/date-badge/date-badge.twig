<div class="sommertheater-date-badge" aria-label="{{ label|default('Event date') }}">
  <span class="sommertheater-date-badge__day">{{ day }}</span>
  <span class="sommertheater-date-badge__month">{{ month }}</span>
  <span class="sommertheater-date-badge__year">{{ year }}</span>
</div>
