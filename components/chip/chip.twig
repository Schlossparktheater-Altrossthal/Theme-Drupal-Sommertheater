{% set classes = [
    'chip',
    'chip--color-' ~ color,
    dismissable ? 'chip--dismissable',
  ]
%}

<div {{ attributes.addClass(classes) }}>
  {% block chip_content %}empty{% endblock %}
</div>
