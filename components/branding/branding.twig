<a href="{{ url }}" rel="home" class="branding hg:w-40 hg:block">
  {% block branding %}
    <img class="hg:object-cover hg:w-full hg:h-full" src="{{ site_logo.src }}" alt="{{ site_logo.alt }}" fetchpriority="high" />
  {% endblock %}
</a>
