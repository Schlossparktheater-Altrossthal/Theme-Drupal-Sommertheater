{% set classes = [
    'card-pricing',
    'flex',
    'flex-col',
    'gap-4',
    'md:gap-6',
    'border',
    'p-6',
    'lg:p-10',
    '2xl:p-12',
    'color-mode--' ~ color_mode,
    promote ? 'promote',
  ]
%}

<article class="{{ classes|join(' ') }}">

  <header class="card-pricing--header flex flex-col gap-2">
    {{ include('mercury:heading',
      {
        level: 3,
        heading_classes: ['card-title'],
      })
    }}

    <p class="card-description">
      {{ description }}
    </p>
  </header>

  <div class="card-pricing--price">
    <h4 class="card-price">
      <data value="{{ price }}" data-currency="{{ currency_code|default('USD') }}">
        {% if symbol_position is defined and symbol_position == 'before' %}
          <sup>{{ currency_symbol }}</sup>{{ price }}
        {% else %}
          {{ price }}<sup>{{ currency_symbol }}</sup>
        {% endif %}
      </data>
    </h4>
  </div>

  <div class="card-pricing--features rich-text">
  {% block richtext %}
    {{ include('mercury:text',
      {
        text: text|raw,
      })
    }}
  {% endblock %}
  </div>

  <footer class="card-pricing--footer mt-auto">
    {{ include('mercury:button', {
      url: button_url,
      label: button_label,
      variant: promote ? 'fill-light' : color_mode == 'dark' ? 'fill-dark' : 'fill',
      radius: 'rounded-sm',
    }) }}
  </footer>

</article>
