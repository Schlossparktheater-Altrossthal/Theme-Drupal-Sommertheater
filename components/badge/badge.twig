{% set classes = [
    'badge',
  ]
%}

<a href="{{ link.url }}" aria-label="{{ link.label }}" class="{{ classes|join(' ') }}">
  <span class="font-extralight text-md leading-none">{{ link.label }}</span>
</a>
