{% set classes = [
    'apply-badge',
    'badge--color-' ~ color,
  ]
%}

<div class="{{ classes|join(' ') }}">
  <a href="{{ link.url }}" aria-label="{{ link.text }}" class="badge__link">
    {{ link.text }}
  </a>
</div>
