{% set classes = classes|default([])|merge([
    'section-header',
    'hg:w-full hg:gap-6',
    'hg:flex hg:justify-between hg:items-baseline',
])|filter(v => v is not empty) %}

<div class="{{ classes|join(' ') }}">
  <div class="">
    {% block heading_slot %}
      {{ include('mercury:heading', { url: '' }) }}
    {% endblock %}
  </div>
  <div class="">
    {% block content_slot %}
      {{ include('mercury:link') }}
    {% endblock %}
  </div>
</div>
