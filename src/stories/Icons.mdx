import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';

export const icons = {
  names: ['home', 'menu', 'close'],
};

<Meta title="Icons" />

# Icons

This page displays the icon set used throughout the design system, showcasing standardized symbols with consistent style and sizing, based on Google's Material Symbols Outlined.

<IconGallery>
  {
    icons.names.map(icon => (
      <IconItem key={icon} name={icon}>
        <span
          className="material-symbols-outlined"
          style={{ color: '#000', fontSize: '24px' }}
        >
          {icon}
        </span>
      </IconItem>
    ))
  }
</IconGallery>
